# go-tutorials

## Run

1. up
   ```bash
   docker compose up -d
   ```
1. exec
   ```bash
   docker-compose exec docker-ebpf /bin/sh
   # inside docker container
   # go run ./cmd/ebpf-xdp-test/
   ```
1. down
   ```bash
   docker compose down
   ```
